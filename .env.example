# =============================================================================
# Second OpenClaw instance — variables for Railway (and local run)
# Copy to .env and set in Railway Dashboard → Variables
# =============================================================================

# --- Railway / runtime ---
PORT=8080
# Railway sets PORT automatically; 8080 is the default for HTTP proxy.

# --- OpenClaw state (persistence) ---
# On Railway: mount a Volume at /data and set:
OPENCLAW_STATE_DIR=/data/.openclaw
OPENCLAW_WORKSPACE_DIR=/data/workspace

# --- Gateway auth (admin secret for Control UI / WebChat) ---
OPENCLAW_GATEWAY_TOKEN=

# --- Setup wizard (browser /setup) — required for first-time config ---
SETUP_PASSWORD=

# --- Models / LLM ---
OPENAI_API_KEY=

# --- Telegram (second bot from @BotFather) ---
TELEGRAM_BOT_TOKEN=

# --- Optional: config path (defaults to $OPENCLAW_STATE_DIR/openclaw.json) ---
# OPENCLAW_CONFIG_PATH=/data/.openclaw/openclaw.json

# --- Optional: Discord, Slack, etc. (add in /setup or here) ---
# DISCORD_BOT_TOKEN=
# SLACK_BOT_TOKEN=
# SLACK_APP_TOKEN=
